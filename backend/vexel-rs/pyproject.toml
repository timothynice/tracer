[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "vexel-rs"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
features = ["extension-module"]
module-name = "vexel_rs"
